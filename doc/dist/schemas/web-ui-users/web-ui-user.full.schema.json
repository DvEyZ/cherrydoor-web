{
    "$schema": "https://json-schema.org/draft-07/schema",
    "title": "Web UI user full response model.",
    "description": "A full representation of an existent Web UI user.",
    "type": "object",
    "properties": {
        "id": {
            "description": "A database ID of the user",
            "type": "integer"
        },
        "name": {
            "description": "An unique username of the user",
            "type": "string"
        },
        "is_admin": {
            "description": "Determines whether the user has admin permissions or not.",
            "type": "boolean"
        }
    },
    "required": [
        "id", "name", "is_admin"
    ]
}