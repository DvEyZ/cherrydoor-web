{
    "$schema": "https://json-schema.org/draft-07/schema",
    "title": "Web UI user creation model.",
    "description": "A representation of a new Web UI user, used to create it.",
    "type": "object",
    "properties": {
        "name": {
            "description": "An unique username of the user",
            "type": "string"
        },
        "password": {
            "description": "A password of the user.",
            "type": "string"
        },
        "is_admin": {
            "description": "Determines whether the user has admin permissions or not.",
            "type": "boolean"
        }
    },
    "required": [
        "name", "password", "is_admin"
    ]
}